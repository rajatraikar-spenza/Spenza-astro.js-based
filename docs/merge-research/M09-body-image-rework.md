# M09 body-image rework

24 September 2026. Four-stop orange palette: #C63600, #FF4500, #FF6F39, #FF8E64. No brown endpoint. Scope: generated body images only.

## Inventory and decisions

### readiness-levels.png

DROP the three claim cards into the consolidated HTML table. Illustrate the non-automatic transitions with separate platforms and closed evidence gates.

Verification at 1024px: Gaps and checkpoints are visible; three Figma-set labels read clearly; platform shadow sides corrected to cool slate.

Replacement: `/blog-media/iot-trends/readiness-evidence-gates.png`. Original PNG and WebP deleted.

1600×900 PNG: 714,725 bytes. WebP: 140,550 bytes.

### operating-layers.png

DROP the three-row text diagram into the consolidated HTML table. Show a profile entering the SIM function inside unchanged radio hardware, separately connected to an application.

Verification at 1024px: The Profile label is beside the hovering tile, not under the antenna. Hardware and application stay distinct. Conceptual cutaway, not a vendor circuit schematic.

Replacement: `/blog-media/iot-trends/device-profile-application-cutaway.png`. Original PNG and WebP deleted.

1600×900 PNG: 720,393 bytes. WebP: 111,128 bytes.

### automation-controls.png

DROP the three control cards into the consolidated HTML table. Illustrate authorization as a barrier before execution, with a verification/recovery return path.

Verification at 1024px: Gate and return loop are visible before labels. Four Figma-set labels are legible without crossing objects or arrows.

Replacement: `/blog-media/iot-trends/authorized-change-recovery.png`. Original PNG and WebP deleted.

1600×900 PNG: 760,037 bytes. WebP: 102,390 bytes.

## Preservation and gate

All three text-card images were deleted. Their nine distinctions are preserved together in one additional HTML table. New illustrations show readiness barriers, hardware/profile separation and controlled automation. Existing prose and all three original tables remain unchanged. All 0 published body figures retain their exact markup and srcset. Every protected hero/other local asset retains its original SHA-256. Information gain stays 7. `npm run merge:qa -- M09`: PASS.

Full-frame grain can exceed the old PNG size heuristic; 256-colour quantization is applied and WebP is served through the existing picture element. Both formats were inspected for crop, legibility and banding.
