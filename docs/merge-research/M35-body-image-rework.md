# M35 body-image rework

24 September 2026. Four-stop orange palette: #C63600, #FF4500, #FF6F39, #FF8E64. No brown endpoint. Scope: generated body images only.

## Inventory and decisions

### form-versus-function.png

DROP two comparison boxes; preserve their criteria in the table and illustrate independent hardware and capability inputs.

Verification at 1024px: Inspected at 1024px: labels readable, orange field, visible object relationships and depth.

Replacement: `/blog-media/iot-sim-cards/package-and-profile-capability.png`. Original PNG and WebP deleted.

1600×900 PNG: 774,740 bytes. WebP: 86,830 bytes.

### lifecycle-record.png

DROP four lifecycle boxes; preserve the checkpoints in the same table and illustrate record continuity across changing service stages.

Verification at 1024px: Inspected at 1024px: labels readable, orange field, visible object relationships and depth.

Replacement: `/blog-media/iot-sim-cards/subscription-record-continuity.png`. Original PNG and WebP deleted.

1600×900 PNG: 830,675 bytes. WebP: 116,400 bytes.

## Preservation and gate

Only replacement tables and figure metadata change. Original prose, existing tables and published images are unchanged. The generated profile-action menu was rejected and removed; generic profile objects carry no claim about supported operations. All 1 published body figures retain their exact markup and srcset. Every protected hero/other local asset retains its original SHA-256. Information gain stays 7. `npm run merge:qa -- M35`: PASS.

Full-frame grain can exceed the old PNG size heuristic; 256-colour quantization is applied and WebP is served through the existing picture element. Both formats were inspected for crop, legibility and banding.
