# M39 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## documentation-to-fleet-evidence.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Illustrate how provider documentation becomes fleet-specific acceptance evidence. NO text or numbers. Full orange colour field with the approved four-stop ramp, grain, soft glow and blue-grey/off-white objects. Left an open technical manual with abstract interface symbols and a carrier contract scroll. Centre a magnifying glass over an actual SIM-equipped field device on a testing bench, linked to a carrier antenna. Right a dossier with trace waveform, device icon and an invoice sheet gathered by one binder ring. A broad open directional arc runs from published pages through the testing bench to collected evidence, with a clearly visible inspection gap so documentation is not a guarantee. Three-dimensional depth through hand-drawn shading, not shiny rendering. No rankings, checkmark seals, provider logos or data values. Reserve bottom quarter as clear orange space for three short labels.

Figma labels: Published scope, Fleet-specific test, Collected evidence

## request-state-and-reconciliation.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Illustrate an asynchronous connectivity API operation with a visible retry and reconciliation loop, NO text or numbers. Bright orange textured field. Left a key-authorized request envelope with a distinct circular tag travels toward a small server in centre-left. An acknowledgment receipt branches back toward the request; separately, a path continues from server to a subscription-chip switch and a small clock in centre-right, then splits toward a connected sensor application and an invoice sheet on right. A retry arc circles near the same tagged envelope to imply correlated retries, not a second purchase. Main takeaway: acknowledgment is separate from observed state and application/billing evidence. Strong directional arrows, distinct objects, no boxed steps, no dashboard metrics or universal capability claims. Keep lower quarter clear for labels. Approved orange ramp and slate/off-white objects only.

Figma labels: Request, Acknowledgment, Observed state, Reconcile
