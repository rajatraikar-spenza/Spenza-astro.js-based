# M35 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## package-and-profile-capability.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Independent physical package and profile capability decisions for IoT SIMs. A central sensor enclosure is open. On its left, a caliper and assembly tray hold both a removable SIM card and a small soldered chip; on its right, a separate permission-key mechanism governs profile packets and supported lifecycle actions. Two independent spokes meet the device, with no arrow or automatic implication from the physical package to remote operations. Clear hardware versus authorized-function relationship through real objects. Full bright orange field, slate/off-white objects, grain/glow/shadow. No brown landscape or text. Leave bottom y=790..900 clear.

Figma labels: Physical package, Profile capability

## subscription-record-continuity.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A subscription record as a continuous durable thread running through four physical stages: an inventory cabinet holding a tagged SIM and device, an activated sensor exchanging a message with an application, a replacement-profile tool with a recovery arrow, and a retired device whose final invoice is checked and archived. The same distinctive round tag stays on the record thread throughout; requested changes sit on small loose slips while confirmed results attach to the thread. No invented identifiers, dates, counts or UI. Bright orange full frame, slate/off-white objects, grain/glow/shadow, no brown or words. Reserve bottom y=790..900.

Figma labels: Inventory, Activate, Change, Retire
