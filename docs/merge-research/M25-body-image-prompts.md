# M25 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## separate-and-pooled-allowances.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Compare data allowance boundaries through objects: left three sensors each pour data-dot streams into their OWN small measuring vessel, separated and not connected; right the same three sensors pour streams into ONE shared larger vessel. Small lip overflow arrows show overage is individual on left and shared on right. This is an unquantified conceptual comparison, no savings claim, no numeric levels. Composition must show separate versus pooled containment before reading any text. Bright orange full field, slate vessels/sensors, off-white streams, grain/glow/shadow. No landscape/brown, boxes, numbers or words. Reserve top y=20..110 and bottom y=790..900.

Figma labels: Individual allowances, Shared allowance

## invoice-audit-thread.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Invoice reconciliation illustrated as a continuous audit thread passing through a traffic meter, a contract-rule gear, a paper invoice, and a magnifying lens. The same distinctive round identifier token appears along the thread at each object so records can be correlated. A small branch loops from the lens back to the contract when a mismatch is found. Do not invent charges, rates, actual records or product screenshots. Full bright orange field, off-white invoice and thread, slate instruments, grain/glow/shadow. No landscape/brown, numbers or text. Leave bottom y=790..900 clear for labels.

Figma labels: Usage, Rating, Invoice, Review
