# M42 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## profile-network-application-observations.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Illustrate three independent observations needed to verify an eSIM profile change, NO text or numbers. A single conceptual device in the lower centre is opened to reveal its eUICC, with a small new profile token being inserted from upper left. Upper left a magnifying lens inspects the profile on the chip. Upper centre a separate lens observes a carrier antenna and a live data path. Upper right a third lens inspects an application server receiving a device message. Three distinct evidence threads converge into an open campaign dossier at lower right, with exactly three observations to show closure requires all of them, not just a request acknowledgment. Clear nesting and converging paths, not a row of boxed icons. No automatic failover claims, no standards architecture labels or protocol arrows, no product logos, no stats. Full bright four-stop orange field with grain, glow and blue-grey/off-white dimensional objects. Reserve lower strip for concise labels later. This is an acceptance concept, not a literal GSMA architecture.
Final edit: retain exactly three dossier rows and reserve a clear orange lower strip for labels.

Figma labels: Profile result, Network service, Application result
