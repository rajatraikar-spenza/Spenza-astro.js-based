# M31 image plan

Three source assets inspected. Keep destination hero by published URL; reject donor hero and square infrastructure scene. Three body figures earn distinct roles: record joins, change-confirmation lag and pooled/per-line oversight. Four images total including hero. Built-in generation, PNG/WebP deliverables.

## mobile-service-record-joins

```text
Create a rich editorial illustration for a B2B telecom brand. **Fill the frame with a warm colour field** — a soft gradient built from Spenza orange `#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white background. Add a fine grain or paper texture across the whole image and a soft radial glow behind the focal subject so it sits forward. Shapes are confident and slightly hand-drawn rather than mechanically precise, with layered depth: a shadow side, a lit side, and a subtle highlight. Icons and objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with blue-grey `#7B8898` for secondary detail. Generous negative space around a single clear focal point. No stock-photo people, no clip-art, no flat outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels, 16:9. Any text is large, minimal and correctly spelled. 1600x900 landscape. Full orange colour field C63600 FF4500 FF6F39 FF8E64; no brown or9B2A00. Sculptural objects, cool slate supporting surfaces, grain, glow and shadows. Bold geometric sans, exact supplied text only, ink282829 on generous off-white FAFAFA plaques for ALL text. No text directly on orange, no fine print, no decorative slogans or scenery, no outlined text boxes, no white background.Three large objects on left in a vertical arc: employee badge labelled EMPLOYEE, SIM chip labelled SUBSCRIPTION, signed contract scroll labelled CONTRACT. Three distinct thick paths converge into a single dimensional open ledger at center labelled SERVICE RECORD. From ledger one clear path reaches an invoice document at right labelled MATCH THE BILL. Show the convergence clearly, not just a row of cards. Header ONE LINE, LINKED RECORDS. No numbers or personal data.
```

## mobile-change-three-states

```text
Create a rich editorial illustration for a B2B telecom brand. **Fill the frame with a warm colour field** — a soft gradient built from Spenza orange `#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white background. Add a fine grain or paper texture across the whole image and a soft radial glow behind the focal subject so it sits forward. Shapes are confident and slightly hand-drawn rather than mechanically precise, with layered depth: a shadow side, a lit side, and a subtle highlight. Icons and objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with blue-grey `#7B8898` for secondary detail. Generous negative space around a single clear focal point. No stock-photo people, no clip-art, no flat outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels, 16:9. Any text is large, minimal and correctly spelled. 1600x900 landscape. Full orange colour field C63600 FF4500 FF6F39 FF8E64; no brown or9B2A00. Sculptural objects, cool slate supporting surfaces, grain, glow and shadows. Bold geometric sans, exact supplied text only, ink282829 on generous off-white FAFAFA plaques for ALL text. No text directly on orange, no fine print, no decorative slogans or scenery, no outlined text boxes, no white background.A curved left-to-right path through three large sculpted objects: approval stamp/document APPROVED, radio tower with acknowledgement check PROVIDER CONFIRMED, invoice/calendar BILL VERIFIED. Use small hourglass objects visibly between stages to show they are not simultaneous, no duration or time estimate. The approval object must not have a completed invoice inside it. Header A REQUEST IS NOT THE RESULT. Ribbon brightens along four-stop ramp; all labels on light plaques. Show objects and path, not text boxes.
```

## shared-data-pool-visibility

```text
Create a rich editorial illustration for a B2B telecom brand. **Fill the frame with a warm colour field** — a soft gradient built from Spenza orange `#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white background. Add a fine grain or paper texture across the whole image and a soft radial glow behind the focal subject so it sits forward. Shapes are confident and slightly hand-drawn rather than mechanically precise, with layered depth: a shadow side, a lit side, and a subtle highlight. Icons and objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with blue-grey `#7B8898` for secondary detail. Generous negative space around a single clear focal point. No stock-photo people, no clip-art, no flat outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels, 16:9. Any text is large, minimal and correctly spelled. 1600x900 landscape. Full orange colour field C63600 FF4500 FF6F39 FF8E64; no brown or9B2A00. Sculptural objects, cool slate supporting surfaces, grain, glow and shadows. Bold geometric sans, exact supplied text only, ink282829 on generous off-white FAFAFA plaques for ALL text. No text directly on orange, no fine print, no decorative slogans or scenery, no outlined text boxes, no white background.A large transparent orange-lit bowl representing a shared data allowance in center. Three separate thick pipes lead from bowl to three different phone objects arranged at different distances around it, each with its own small gauge. No numbers, no precise measurement, no percentage marks. Two off-white caption plaques: SHARED ALLOWANCE above bowl and CHECK EACH LINE below phones. Header POOL VIEW AND LINE VIEW. Every phone draws from same bowl; make common-source relationship clear. Abstract conceptual illustration, not a quantitative data chart.
```