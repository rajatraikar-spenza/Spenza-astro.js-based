# M36 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## product-constraint-gates.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

An OEM device on a product-design bench faces three independent physical gates around it: a radio antenna and power cell gate, a product-qualification stamp and document gate, and a carrier profile/contract gate. A remote profile packet enters only the service gate and cannot bypass the radio or product-qualification gates. The device is a compact sensor, no literal branded design. Convey that downloadable profiles do not remove hardware or approval constraints. Use objects and paths, no text boxes or checkmark claims. Bright orange full field, slate/off-white, grain/glow/shadow. No brown or words. Leave bottom 100px clear.

Figma labels: Radio design, Product approval, Service support

## provisioning-responsibility-handoffs.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Responsibility handoff across an OEM factory jig, a provisioning service cloud with order dossier, and a carrier antenna. A single tagged device order is passed along a broad path from factory through the workflow to authorized network service. Under each handoff a distinct support tool is tethered to the responsible station, so failures return to the right station. No person portraits or product screenshot. Bright orange full field, slate/off-white objects, grain/glow/shadow. NO words, letters, numerals or brown. Leave bottom 100px clear.

Figma labels: OEM, Platform partner, Carrier

## hardware-to-paying-service.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Three separate commercial milestones for a connected product: a device passing a compatibility gauge at left, the same device exchanging an application message through a lit antenna in the middle, and a recurring payment coin entering a ledger at right. Two visible checkpoints interrupt the connecting path so eligible hardware is not automatically working or paying service. A small uncompleted branch at each checkpoint remains visibly open. No funnel percentages, relative cohort sizes or monetary figures. Bright orange full field, slate/off-white objects, grain/glow/shadow. No brown or text. Leave bottom 100px clear.

Figma labels: Eligible devices, Working service, Paying service
