# M32 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## factory-to-retirement.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A connected-device fleet lifecycle as a continuous curved conveyor. Follow the same distinctive sensor through a factory testing jig, a warehouse shelf, an installed building site, then a retirement station with the network switch off and its history folder safely archived. Four settings, linked by an unmistakable directional path. A lifecycle rather than four isolated cards. No actual customer scale, timestamps or metrics. Full bright orange colour field, slate machinery and off-white sensor/path, grain, glow and shadows. No brown or landscape mountains. No words or numbers. Reserve bottom y=790..900 for labels.

Figma labels: Factory, Warehouse, Deployment, Retirement

## revenue-and-service-deductions.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

An unquantified contribution-cost illustration: a revenue stream of off-white coins enters from left and divides into four visible branches terminating at an antenna, a software gear, a support headset, and a contract with lock. A remaining stream continues to an open ledger at right. Show deductions and residual contribution without measured widths, amounts or a guaranteed positive margin. Arrange objects around one clear flow, never boxed labels. Full bright orange field, slate objects, grain/glow/shadow. No brown, landscape, words or numerals. Reserve top and bottom 100px for separate labels.

Figma labels: Revenue, Access, Platform, Support, Commitments, Contribution

## cohort-acceptance-thread.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A pilot acceptance chain with four distinct physical objects joined by one evidence thread: a contract dossier at left, an operating IoT sensor, an application server receiving a visible message, and a matching invoice under a magnifier at right. Carry the same unique abstract circular tag through every object to show the same cohort being tested and billed. Small diagnostic branch from the sensor to a spanner. Conceptual validation, not a measured customer result. Bright orange full field, slate/off-white objects, grain/glow/shadow. No brown, numbers or text. Leave bottom y=790..900 clear.

Figma labels: Contract, Device, Application, Invoice
