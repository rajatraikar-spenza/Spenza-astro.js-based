# M29 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## subscription-and-firmware-controls.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A large central cutaway IoT sensor with two clearly distinct internal objects: a connectivity SIM/eUICC resource at lower left and a firmware processor/gear at upper right. A subscription console represented by antenna plus SIM stack at far left has a solid control path ONLY to the connectivity resource. A separate firmware tool represented by spanner and software-package icon at far right has a dotted path ONLY to the processor. No bridge suggesting either controller owns the other subsystem. Clear boundaries within the device. No text or numerals, no literal product UI. Full bright orange field, slate/off-white components, grain/glow/shadow, no landscape/brown. Reserve top y=20..110 and bottom y=790..900.

Figma labels: CMP, Device management, Subscription resource, Firmware

## provider-state-and-service-validation.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A change request as a permission-key envelope passing through a provider gateway. After the gateway a record with a clock is inspected by a magnifier, then a sensor's delivered message is checked at an application endpoint. Show two different checkpoints: provider final state and actual service outcome, separated by a visible gap and distinct objects. A failed branch leads to a support tool rather than an automatic success checkmark. No invented data, text or numerals. Full bright orange field, slate/off-white objects, grain/glow/shadow, no landscape/brown. Leave bottom y=790..900 clear.

Figma labels: Request, Provider, Confirm, Validate
