# M01 revised body-image prompts

Generated with the built-in image_gen tool. Artwork generated without text; Inter Bold labels were set separately in Figma (file QYBV0dwgiLrqR4PP6RpgP6, frames 55:2 and 55:6) and composited during optimization. The raster-upload endpoint timed out, so Figma supplied transparent editable typography layers. Heroes and published media assets were excluded.

## Device cutaway

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Composition brief: An editorial cutaway of one connected sensor, occupying the middle two-thirds of the canvas, with the protective housing opened away to reveal two distinct parts side by side. At left inside the device: an eUICC secure chip holding a small stack of operator-profile sheets, with one new sheet moving into that stack along a curved download arrow from above. At right inside the same device: a separate modem circuit with an antenna and radio waves pointing outward. Show a physical connecting trace between the secure chip and modem. The inserted profile is bright pale orange, the stable modem and antenna are ink/slate with off-white highlights. The visual message is that the profile changes inside a secure chip while the radio hardware remains in place. This is a conceptual cutaway, not a manufacturer's circuit diagram. Warm gradient fills all edges, ink shadow under the device, grain and soft glow; rich 2D editorial printmaking, not isometric 3D. Keep the top 170 pixels free for a title and the bottom 150 pixels free for three short labels to be typeset separately. Exact later labels will be "Profile changes", "eUICC", "Modem stays". DO NOT render any text, letters or numbers in this artwork. Do not show multiple interchangeable modems, boxes containing labels, a phone UI, a network tower inside the sensor, a detached SIM card tray, logos or ornamental dashboard cards.

Final labels: “Profiles change. Hardware stays.”, “eUICC + profiles”, “Modem + antenna”. The cutaway is conceptual, not a product-specific physical architecture.

## Recovery loop

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Composition brief: A wide two-dimensional editorial process illustration of safe remote profile-change recovery. One clear branching path readable without labels. Main route travels left to right across the upper-middle of the canvas: a bold SIM-profile chip object on the left at roughly x300 y360, then a radio mast and small application-cloud object together at x800 y360, then a large simple off-white tick in a circular seal at x1320 y360. A strong curved arrow connects left chip to center test, and a curved bright arrow connects center test to right tick. From the CENTER test, a SECOND route bends down toward a magnifying-glass over a retained-profile chip at x800 y700, then bends back up LEFT to the starting chip. Arrowheads make the return loop unambiguous; the success path never loops backwards. The top route brightens along the orange ramp; the recovery return is cool blue-grey/ink with off-white highlights, on the same full-frame burnt-orange-to-coral field. Minimal bold objects with hand-inked edges, printed shadows and lit faces, warm radial glow behind the success seal, fine grain. Keep a generous clear top strip (y0 to170) for a heading. Keep clear label zones directly below the top objects (y510 to580), and beside/below the lower recovery object (y780to845). Exact labels will be added later: "Change profile", "Verify service", "Accept", "Inspect + recover". Render NO text, NO numbers, NO logo. No cards, no labelled boxes, no decorative data dashboard, no artificial performance metrics, no tiny network diagram. Flat painted illustration with tangible layered paper/ink texture, NOT photorealistic hardware, NOT 3D rendering. The route is a proposed operating decision flow, not a protocol architecture.

Final labels: “Verify service before accepting change”, “Change profile”, “Verify service”, “Accept”, “Failed / unknown”, “Inspect + recover”. This is a proposed operating decision flow, not an assertion of universal automatic rollback. The caption retains that qualification.
