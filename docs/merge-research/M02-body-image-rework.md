# M02 body-image rework

24 September 2026. Four-stop orange palette: #C63600, #FF4500, #FF6F39, #FF8E64. No brown endpoint. Scope: generated body images only.

## Inventory and decisions

### mvno-network-and-operating-roles.png

REBUILD the role map. Unlike a checklist, its solid commercial links and dashed optional dependencies carry a spatial relationship. Replace every labelled rectangle with the corresponding phone, storefront, mast, server or contract object; retain its topology.

Verification at 1024px: Five readable role names, clear solid and dashed links; optional suppliers remain outside the main relationship. Full orange field, grain and shadow sides.

1600×900 PNG: 805,342 bytes. WebP: 119,476 bytes.

## Preservation and gate

Article HTML is byte-identical: no prose, captions, tables, author or score changed. All 2 published body figures retain their exact markup and srcset. Every protected hero/other local asset retains its original SHA-256. Information gain stays 7. `npm run merge:qa -- M02`: PASS.

Full-frame grain can exceed the old PNG size heuristic; 256-colour quantization is applied and WebP is served through the existing picture element. Both formats were inspected for crop, legibility and banding.
