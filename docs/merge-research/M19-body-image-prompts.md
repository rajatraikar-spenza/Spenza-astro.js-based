# M19 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## funding-flows.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Show three alternative sources funding one mobile connection: a customer wallet at left, an advertising megaphone above, and a partner shop at right. Three separate coin trails point toward one central phone and antenna. Distinct paths show alternatives, not additive guaranteed revenue. No numeric values or measured flow widths. Orange field all four approved stops, slate objects and off-white coins with ink shadows; no brown. No text in artwork. Reserve bottom strip y=785..900 for separate typography.

Figma typography: Customer, Advertiser, Partner

## observation-to-pilot.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Show a source document under a magnifying lens on the left, a translucent idea/lightbulb in the centre, and an instrumented phone testing bench on the right. A clear narrow ribbon connects source to hypothesis to experiment. The centre thought is provisional, not a fact; use a translucent halo rather than a checkmark. No data points, no charts, no outcome claims. Bright orange field, shadow and grain. Absolutely no letters, numbers or text. Reserve bottom strip y=785..900.

Figma typography: Observe, Interpret, Test

## enterprise-call-and-control.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Conceptual enterprise calling and administration illustration, not a literal customer deployment diagram. A phone with native handset icon at left sends a visible voice-wave ribbon through a network gateway at centre toward a business application screen at right. Above, an administration key points with a separate dotted control line to grouped account icons. Visually distinguish call transport from administration. No vendor logos, no invented UI, no numbers, no text. Bright orange field with slate objects and off-white signal ribbon, grain and shadow. Reserve bottom strip y=785..900.

Figma typography: Native calls, SIP routing, Application, Administration
Correction: replace desktop telephone with mobile smartphone.
