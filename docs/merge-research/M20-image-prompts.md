# M20 image prompt

Built-in generation for one body illustration. Published donor hero referenced without a local copy.

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame with a warm colour field** — a soft gradient built from Spenza orange `#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white background. Add a fine grain or paper texture across the whole image and a soft radial glow behind the focal subject so it sits forward. Shapes are confident and slightly hand-drawn rather than mechanically precise, with layered depth: a shadow side, a lit side, and a subtle highlight. Icons and objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with blue-grey `#7B8898` for secondary detail. Generous negative space around a single clear focal point. No stock-photo people, no clip-art, no flat outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels, 16:9. Any text is large, minimal and correctly spelled. A nested administrative relationship, not a row of cards. One large support headset with a key above three separate circular protected customer islands. Each island contains a small office building, a handset and a receipt as a unified object group. Separate branching paths from the headset enter each island through its own small lock gate; no arrows connect the islands to one another. Exact labels: "MSP" above the headset, "CUSTOMER A", "CUSTOMER B", "CUSTOMER C" beneath the islands. Labels in ink #282829 on small solid off-white #FAFAFA patches, never primary orange. White circular boundaries with orange shading illustrate separate scopes; do not imply a security certification. Orange full-frame field, soft glow, confident editorial shapes, no scenery, no rectangular text containers, no screenshots, no numerical claims. Never use #9B2A00.