# M08 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## combined-supplier-functions.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A single supplier illustrated as two interlocking large gears on a bright orange field. Left gear carries a server rack and a small operational spanner; right gear carries bundled wholesale contracts and a small radio mast. The gears visibly mesh, showing that technology enablement and wholesale aggregation can coexist. Neither side wins and neither is subordinate; equal size and importance. Off-white gear faces, ink shadow sides, cool blue-grey secondary detail; four-stop orange focal glow with fine grain. No boxed panels, no metrics, no text anywhere. Leave bottom y=750..900 for two separately typeset labels "MVNE" and "MVNA", at x=350 and x=1110. The gears are a role metaphor, not a network or legal architecture. No brown or gold.

## cellular-voice-application-path.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A compact editorial call-path illustration: at left one smartphone with a native telephone receiver symbol beside a small cellular mast; a broad curved stream of sound-wave marks leaves the mast and passes through a small gateway connector at centre before entering an application cloud containing a conversation waveform at right. One coherent left-to-right path. This illustrates cellular voice connected to an application via SIP, not the actual customer topology. No portal, no human figures, no customer logos, no invented measurements. Off-white and ink objects, blue-grey gateway, vivid four-stop orange field with grain, focal glow and short shadow sides. Objects in y=150..650, leave bottom y=750..900 blank for typeset labels "Cellular voice", "SIP routing", "Application". No text baked into the art. No brown or ochre.
