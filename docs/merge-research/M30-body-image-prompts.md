# M30 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## profile-delivery-and-management.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Conceptual IoT remote-profile roles, not a full protocol diagram. Left a secure profile-preparation vault containing small sealed profile packets. Right an IoT device cutaway containing a local provisioning gear and a secure chip holding profiles. A broad off-white profile-delivery ribbon links the vault to the device. Above the device a separate permission key/controller has a thin dotted management path to the device. Clearly different delivery and authorization paths. Do NOT put the management controller in the middle of the delivery path. No smartphone QR onboarding, no people. NO text or letters. Bright orange field, slate/off-white objects, grain/glow/shadow, no landscape or brown. Reserve bottom y=790..900 and top-left y=20..110.

Figma labels: SM-DP+, IPA + eUICC, eIM, Profile delivery

## deployment-and-recovery-loop.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

An IoT device acceptance loop on a testing workbench. Left an exact hardware board and firmware gear are checked by a caliper. Centre a secure profile packet slots into the sensor and a signal reaches an application endpoint. Right a deliberately unplugged connection beside a spanner forces a recovery test; a curved return path brings the same sensor back for verified operation. Distinguish successful installation from the separate failure-and-recovery exercise. No claimed durations, percentages or outcomes, no text/numbers. Bright orange full field, slate/off-white objects, grain/glow/shadow, no landscape/brown. Keep bottom y=790..900 clear.

Figma labels: Readiness, Provisioning, Recovery
