# M27 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## identity-and-resource-assignments.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A stable central subscription identity represented by a solid circular anchor medallion. A customer-account icon connects to it from the left. On the right two replaceable resource objects, a SIM card and a telephone-number tag with only a handset symbol, orbit on detachable clips; a faded former SIM moves outward as a new SIM moves inward. A small history scroll remains tethered to the central anchor. Make enduring identity versus changing assignments visually explicit. No numeric identifiers or letters. Bright orange field, slate/off-white objects, grain/glow/shadow, no landscape or brown. Keep bottom y=790..900 clear.

Figma labels: Customer, Subscription, Resources, Assignment history

## operation-outcome-reconciliation.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A telecom operation progressing from a sealed request envelope at left, through a provider gateway with an hourglass at centre, to two records being aligned under a magnifying lens at right: one commercial ledger and one network-service antenna record. A return acknowledgement arrow goes from gateway toward request; a separate forward path reaches the reconciliation lens only after outcome evidence. An uncertain branch ends at a diagnostic spanner, not an automatic retry loop. No numbers, no success rates, no actual UI, NO text. Full bright orange field, slate/off-white objects, depth and grain, no landscape/brown. Keep bottom y=790..900 clear.

Figma labels: Requested, Confirmed, Reconciled
