# M33 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## independent-sim-decisions.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Three independent decisions around one central device: above-left a caliper measures a removable SIM and a soldered chip, above-right a profile packet rotates through a gear representing supported operations, below a carrier antenna is tied to a service contract. Each independently connects to the central device on its own spoke, no arrows between the three decisions and no claim that shape determines capability or coverage. Use bold dimensional objects, not text cards. Bright orange full field, slate/off-white shapes, grain/glow/shadow, no brown. NO text/numbers. Leave clear label spaces near outer corners.

Figma labels: Form factor, Capability, Service

## physical-and-remote-service-paths.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A practical deployment fork: one route reaches an accessible device with an open SIM tray and a replacement card moved by a small service tool; another route reaches a sealed sensor receiving an authorized profile packet remotely from a cloud. A third central mixed-device group has both service paths available. Make physical intervention and remote provisioning visually different, without claiming all devices support both. Full bright orange field, slate/off-white objects, depth and grain, no brown, words or numbers. Reserve bottom y=790..900 for typography.

Figma labels: Physical access, Hybrid fleet, Remote profiles

## whole-lifecycle-costs.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Whole-lifecycle costing as a single winding ribbon following one device from an unpacked hardware kit and integration gear at left, through a carrier antenna and support headset at centre, to a retired device and closed final ledger at right. At each stage a separate small coin tray attaches to the ribbon, and thin threads from all trays converge into one total-cost ledger. No prices, relative amounts or savings claims. Show costs throughout the journey rather than only the initial card. Orange full field, slate/off-white objects, grain/glow/shadow. No brown or text. Reserve bottom y=795..900.

Figma labels: Acquire, Operate, Retire, Total lifecycle cost
