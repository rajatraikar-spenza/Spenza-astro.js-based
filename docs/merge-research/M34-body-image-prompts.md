# M34 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## identity-versus-data-route.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Local subscription identity versus actual data routing, shown with two abstract geographic zones, not real countries. In the left zone a sensor carries a small subscription badge linked to a local antenna. A luminous traffic path travels from that antenna across the zone boundary to a network exit gateway and remote application server in the right zone; smaller branches reach logs and backup storage. Identity badge stays local while data crosses the boundary. No claim that all local profiles route remotely: this is an illustrative possibility. Bright orange field, slate/off-white objects, grain/glow/shadow. No real map, flags, text, numbers or brown. Keep bottom 100px free.

Figma labels: Local identity, Verify the full data path

## deployment-record-review.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Deployment-specific country records illustrated as separate folders tethered to three distinct abstract map pins and their devices. One foreground folder is opened to reveal a service contract icon, a calendar with no numbers, an owner key and a magnifying lens. A circular review arrow links a changed device/plan icon back to that folder. Make the relation one deployment scope to one maintained record obvious. Not a legal approval or coverage map; no checkmarks implying approval. Full bright orange field, slate/off-white objects, grain/glow/shadow, no brown, words or numbers. Keep bottom 100px clear.

Figma labels: Scope-specific records, Review on change

## alternate-path-and-application-proof.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Application-level recovery in a network change. At left a sensor's first route to an antenna is visibly broken; an alternate curved route reaches a second eligible antenna. Both paths join a shared central network gateway before reaching a message inbox at right under a magnifying lens. Show the shared dependency, then explicit inspection of received messages for gaps and duplicate envelopes. No instant or guaranteed failover, no latency numbers. Full bright orange field, slate/off-white objects, grain/glow/shadow, no brown, words or numbers. Leave bottom y=790..900 clear.

Figma labels: Failure, Alternative, Shared dependency, Verify receipt
