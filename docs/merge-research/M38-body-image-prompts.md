# M38 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## municipal-service-response-loop.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Create a municipal service acceptance illustration, NO text or numbers anywhere. Full bright orange colour field and dimensional grain. Left underground water meter in a cutaway street; a glowing message token travels upward to a cellular antenna in centre-left, continues to a municipal application console centre-right, then to a maintenance worker with spanner on right. One visible curving path connects the four objects. A thinner return arc from worker toward installed meter shows response. Network registration is an intermediate point along the route, not the endpoint. No dashboards full of fake metrics. Keep objects in upper two-thirds; reserve clear orange lower strip for four short labels added later. Use only approved orange ramp and blue-grey/off-white objects, no brown.

Figma labels: Installed site, Network, Application, Operations

## municipal-control-cutaway.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Create an explanatory municipal streetlight cutaway, NO text or numbers anywhere. Full bright orange field, soft glow, grain and shaded blue-grey objects. In centre a streetlight controller opened to show a small subscription chip and a separate firmware circuit board. From upper left, a carrier antenna with usage ledger connects only to the subscription chip via a narrow route. From upper right, a technician's update tool and key connect to the firmware board. Far right a municipal application server with a key and archive connects to a lamp-control message. Make the three ownership paths visibly separate with their own tools, not three panels or cards. A shared municipal asset does not transfer device and application responsibilities to the connection provider. Keep bottom quarter clear for labels. No locks that imply a security certification, no logos.

Figma labels: Connectivity, Device, Application
