# M15 image prompt

Built-in generation, one new body image. Existing published hero reused by URL.

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame with a warm colour field** — a soft gradient built from Spenza orange `#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white background. Add a fine grain or paper texture across the whole image and a soft radial glow behind the focal subject so it sits forward. Shapes are confident and slightly hand-drawn rather than mechanically precise, with layered depth: a shadow side, a lit side, and a subtle highlight. Icons and objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with blue-grey `#7B8898` for secondary detail. Generous negative space around a single clear focal point. No stock-photo people, no clip-art, no flat outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels, 16:9. Any text is large, minimal and correctly spelled. A rising curved path joining four bold objects: a payment receipt, an eSIM chip, a handset with a download arrow, then a handset radiating a signal. Exact labels beneath: "PAID", "ALLOCATED", "INSTALLED", "CONNECTED". A smaller separate headset below the path receives a downward branch from the chip and handset, with label "SUPPORT". Each main label is ink #282829 on an off-white #FAFAFA small unoutlined paint patch; never place type on primary orange. The arrows show sequence; the support branch shows recovery, not a second purchase. Abstract editorial composition with no scenery, no landscape, no buildings beyond these conceptual objects. No numbers, no time promises, no fake software UI, no QR code, no boxes. Never use #9B2A00.