# M09 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## readiness-evidence-gates.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

An editorial readiness obstacle course, no checklist cards. Three substantial stepping stones across a gap: first holds an open technical blueprint book, second a packaged sensor device with a purchase tag, third a working sensor in a small field-test rig with a magnifier. Each adjacent pair is separated by a visible break and a small closed checkpoint gate: having the first does not automatically establish the next. Horizontal progression, equal visual importance, no numerical scale or time axis. Bright four-stop orange field, cool-grey gates, off-white objects, ink shadow sides and grain. Keep objects above y=680 and reserve bottom for labels "Standard", "Offer", "Field proof". NO text in art, no brown, no scenery that obscures the gaps.

Revision: remove all generated typography; apply Inter Bold labels separately in Figma.

## device-profile-application-cutaway.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

An exploded cutaway of an IoT product showing separate domains. Bottom: a large physical modem circuit board and antenna. Above it: a small secure SIM chip inside the device with a detachable profile tile hovering immediately above the chip; the tile connects to that chip alone by a short arrow. Far right: an application cloud receiving a distinct dotted service link from the antenna. The remote profile tile must NOT replace or transform the modem board. Spatial nesting and separation are the message. Bright orange field, off-white chip and tile, cool slate board, ink components, glowing edge highlights and fine grain. No text or labels in the art. Reserve lower y=760..900 for labels "Device + radio", "Profile", "Application". No brown or gold; contact pads can be off-white.

Revision: remove all generated typography; apply Inter Bold labels separately in Figma.

## authorized-change-recovery.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A visible authorization barrier in an automated-change workflow. Left a recommendation document with a lightbulb approaches a substantial closed permission gate with a keyhole at centre. Right of the gate an operational server with a SIM icon can be reached only through that gate. Below the server, a magnifier points back toward its status, and a curved return arrow loops to a recovery spanner at bottom centre. This is a controlled change with verification, not an autonomous guarantee. Strong readable arrows, no boxes, no text in the art. Leave y=50..130 for three labels "Recommend", "Authorize", "Execute"; y=790..870 for "Verify + recover". Four-stop orange field, slate gate, off-white objects, grain and short ink shadow sides. No brown, beige, red/green signal colours or customer logos.

Revision: remove all generated typography; apply Inter Bold labels separately in Figma.
