# M21 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## separable-service-contributions.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

An exploded three-part service assembly: a software gear and database at left, two interlocking connector plugs at centre, a support headset with spanner at right. Each connects toward a central mobile-service antenna, but each remains visibly separable with independent attachment joints. This is three separately contracted contributions, not a guaranteed all-inclusive bundle. Use full bright orange field, off-white gear, slate connectors/headset, distinct shadow sides and glow. No boxes, text or numbers. Reserve bottom y=795..900 for typography.

Figma labels: Software, Integration, Operations

## incident-context-loop.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Incident handoff illustrated as one distinctive tagged dossier travelling from a mobile phone showing a warning at left, through a brand service desk magnifying the same dossier at centre, to a technical spanner and diagnostic instrument at right. A separate return arrow loops below to the same phone with a check, showing recovery must be confirmed. The distinctive dossier tag is an abstract circle, no identifier. Not a measured incident. Full bright orange field, off-white arrows and dossier, slate objects and ink shadows. NO letters or numbers. Keep bottom y=805..900 clear.

Figma labels: Customer, Brand, Resolver, Confirm recovery

## reseller-account-hierarchy.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Nested reseller account administration hierarchy illustrated using a master key above a branching mobile over three distinct account clusters, each containing its own small mobile devices on separate circular bases. Fine control tethers follow the branch topology without crossing between accounts. This is a conceptual hierarchy, no actual customer counts, no literal product UI. Use strong full orange field, slate keys and phones, off-white branch paths, soft shadows/grain. NO text or numerals. Reserve bottom y=800..900 for typography.

Figma labels: Reseller, Separate customer accounts
