# M07 body-image rework

24 September 2026. Four-stop orange palette: #C63600, #FF4500, #FF6F39, #FF8E64. No brown endpoint. Scope: generated body images only.

## Inventory and decisions

### white-label-mvno-responsibility-roles.png

DROP the four role cards and transcribe their scope into a table. Replace the implied serial chain with a contract-centred map: agreed roles connect each participating entity.

Verification at 1024px: Five labels readable, each tether reaches a separate contract tab; no serial traffic implication.

Replacement: `/blog-media/white-label-mvno/contract-role-allocation.png`. Original PNG and WebP deleted.

1600×900 PNG: 877,147 bytes. WebP: 162,682 bytes.

### white-label-mvno-request-to-bill.png

REBUILD the acceptance flow. Its forward sequence and failure/retest loop are useful relationships, so retain both while replacing boxed clip-art with objects on a bright colour field.

Verification at 1024px: Four steps and the failure/retest return remain clear; Inter Bold labels were added in Figma frame 59:2 and inspected at 1024px.

1600×900 PNG: 871,023 bytes. WebP: 121,562 bytes.

## Preservation and gate

Responsibility cards were transcribed into a fourth HTML table and replaced with a contract-centred role map. The acceptance loop was rebuilt without changing its information. Original paragraphs, lists, headings and existing tables remain unchanged. All 1 published body figures retain their exact markup and srcset. Every protected hero/other local asset retains its original SHA-256. Information gain stays 7. `npm run merge:qa -- M07`: PASS.

Full-frame grain can exceed the old PNG size heuristic; 256-colour quantization is applied and WebP is served through the existing picture element. Both formats were inspected for crop, legibility and banding.
