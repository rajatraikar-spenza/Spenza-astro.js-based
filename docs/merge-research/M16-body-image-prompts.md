# M16 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## independent-product-scope-choices.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Two independent physical selector dials, illustrating that product use case and operating scope are separate choices. The left dial is ringed by three small objects: a sensor, employee phone, and mobile checkout kiosk. The right dial is ringed by three different objects: branded shopfront, service server and core-network globe. Both dials have independent pointer needles; no lines pair the options across dials, no winner or recommended setting. Large bold off-white circular dials on a four-stop bright orange field, cool-grey icons, ink shadow sides and paper grain. Reserve top y=30..120 for two labels added later. No text, letters, numbers, logos or rectangular cards anywhere. No brown or tan.

Typography stage: "Product use case", "Operating scope" in Figma Inter Bold.

## us-mvno-contribution-and-cash-timing.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

Two connected financial ideas as concrete objects. Left: a bowl of collected-revenue coins feeds a small channel, a cost branch diverts some coins toward a network-and-server object, remainder collects in a shallow contribution tray. Right: two large calendar leaves stand apart on a road, one beside an outgoing supplier invoice, another beside an incoming customer receipt. An empty gap between them is bridged by a wallet, showing funding may be needed even when the left-side contribution remains positive. No dates, no amounts, no numeric proportions, no currency signs or text. Clear separation between arithmetic on left and timing on right; full orange field, off-white coins, cool slate wallet, ink shadows, grain and glow. Reserve top and bottom strips for separately typeset labels. No brown or gold.

Typography stage: "Revenue", "Contribution", "Variable costs", "Supplier paid", "Customer pays", "Funding gap" in Figma Inter Bold.

## mvno-us-launch-10-step-path.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A serpentine ten-stop launch path on a bright orange field. EXACTLY five recognizable objects across the TOP row with centres x=170,480,790,1100,1410 at y=240: target, scope document, radio mast, interlocking partnership rings, legal shield. EXACTLY five objects across the BOTTOM row with centres x=1410,1100,790,480,170 at y=620: systems server, price tag, distribution megaphone, test flask, expanding device group. One curved path travels top-left to top-right, bends down at far right and returns across the lower row right-to-left. Put small direction arrows on the line. The line brightens from #C63600 through #FF4500 to #FF8E64, with off-white edging for contrast. Do not add cards, boxes, extra nodes, text, numbers or title. Keep y=370..435 and y=775..845 blank for labels. Icons have shadow and lit sides, grain and soft glow; off-white and slate on orange. No brown.

Typography stage: "1. Audience", "2. Model", "3. Host", "4. MVNE", "5. Duties", "6. Systems", "7. Pricing", "8. Channels", "9. Pilot", "10. Scale" in Figma Inter Bold.
