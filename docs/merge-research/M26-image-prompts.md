# M26 image prompt

Built-in generation, one body illustration. Existing destination hero referenced.

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame with a warm colour field** — a soft gradient built from Spenza orange `#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white background. Add a fine grain or paper texture across the whole image and a soft radial glow behind the focal subject so it sits forward. Shapes are confident and slightly hand-drawn rather than mechanically precise, with layered depth: a shadow side, a lit side, and a subtle highlight. Icons and objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with blue-grey `#7B8898` for secondary detail. Generous negative space around a single clear focal point. No stock-photo people, no clip-art, no flat outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels, 16:9. Any text is large, minimal and correctly spelled. A curved three-stage journey around a central open gift containing a small eSIM chip. At left a membership pass with a star icon, in centre a detachable reward ticket held above the gift, at right a handset with a strong network signal. Broad arrows visibly travel from membership pass to ticket to handset. Exact labels beneath: "ELIGIBLE", "CLAIMED", "USABLE". A small receipt and magnifying glass below the final handset connects back to the gift with a fine curved feedback arrow, label "MEASURE". No numeral, chart or claim of retention uplift. Text ink #282829 on solid off-white #FAFAFA paint patches. Every concept has an object. Full orange field, grain, soft central glow and painted shadow side, no scenery, no outlined rectangles, no fake UI, no actual QR code. Never use #9B2A00 or text directly on #FF4500.