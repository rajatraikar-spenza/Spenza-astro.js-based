# M43 image plan

Nine source image placements viewed before prompting. Keep destination hero remotely with live srcset. Two new body figures teach independent default choices and shared hardware dependency, unlike the rejected benefit wheels/market chart. Three images total.

## dual-sim-defaults-control

```text
Create a rich editorial illustration for a B2B telecom brand. **Fill the frame with a warm colour field** — a soft gradient built from Spenza orange `#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white background. Add a fine grain or paper texture across the whole image and a soft radial glow behind the focal subject so it sits forward. Shapes are confident and slightly hand-drawn rather than mechanically precise, with layered depth: a shadow side, a lit side, and a subtle highlight. Icons and objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with blue-grey `#7B8898` for secondary detail. Generous negative space around a single clear focal point. No stock-photo people, no clip-art, no flat outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels, 16:9. Any text is large, minimal and correctly spelled. 1600x900 landscape. Full orange colour field C63600 FF4500 FF6F39 FF8E64; no brown or9B2A00. Sculptural objects, cool blue-grey surfaces, grain, soft glow and shadows. Bold geometric sans, exact supplied text only, all text ink282829 on generous off-white FAFAFA plaques. Never text directly on orange. No fine print, extra slogans or scenery, no outlined word boxes, no white background. Objects and visible relationships, not an HTML-table substitute.Large central dimensional phone showing two separate physical selector knobs, top knob has handset and chat-bubble objects next to it, bottom knob has a globe/data-stream object next to it. Plaques beside knobs CALLS / MESSAGES and MOBILE DATA. Two large SIM-profile medallions at left labelled WORK and PERSONAL each have a visible path arriving at both selector controls. From the top selector a path exits right to a handset-and-message object. From bottom selector one single path exits right to a globe object, clearly ONE selected data route, no split into two simultaneously. Header SET EACH DEFAULT. Footer TWO LINES, SEPARATE CHOICES. Conceptual controls, not a fake operating-system screenshot. Never show work apps automatically tied to work SIM.
```

## dual-sim-shared-device

```text
Create a rich editorial illustration for a B2B telecom brand. **Fill the frame with a warm colour field** — a soft gradient built from Spenza orange `#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white background. Add a fine grain or paper texture across the whole image and a soft radial glow behind the focal subject so it sits forward. Shapes are confident and slightly hand-drawn rather than mechanically precise, with layered depth: a shadow side, a lit side, and a subtle highlight. Icons and objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with blue-grey `#7B8898` for secondary detail. Generous negative space around a single clear focal point. No stock-photo people, no clip-art, no flat outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels, 16:9. Any text is large, minimal and correctly spelled. 1600x900 landscape. Full orange colour field C63600 FF4500 FF6F39 FF8E64; no brown or9B2A00. Sculptural objects, cool blue-grey surfaces, grain, soft glow and shadows. Bold geometric sans, exact supplied text only, all text ink282829 on generous off-white FAFAFA plaques. Never text directly on orange. No fine print, extra slogans or scenery, no outlined word boxes, no white background. Objects and visible relationships, not an HTML-table substitute.A large dimensional phone centrally mounted on one shared foundation. Above left a SIM-profile medallion labelled WORK LINE, above right a second medallion labelled PERSONAL LINE. Two thick paths converge into this same phone, where one large battery object is clearly visible. At base a single visible power connector supports the whole phone. Make common dependency visually obvious. Header TWO LINES. ONE DEVICE. Bottom off-white plaque SHARED BATTERY AND HARDWARE. Do not depict separate phones or two batteries, no numbers or uptime percentages, no claim that subscriptions merge or bills combine.
```