# M17 body-image prompts

These prompts use the four-stop palette. Generated with the image_gen tool; no hero or published asset is an input.

## reporting-energy-cycle.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A circular reporting-energy cycle around one central battery-powered sensor. Four large distinct objects around the ring: top-left a radio mast with searching arcs, top-right a message envelope travelling on radio waves, bottom-right an hourglass beside a receiver, bottom-left a crescent moon beside a sleeping sensor. Broad clockwise arrows connect all four, with a small return hook near the radio mast to suggest retries. This is a conceptual measurement cycle, not a chart of relative energy or duration; equal emphasis, no wedges, no proportions. Bright four-stop orange field, off-white objects, cool slate secondary detail, ink shadow sides, fine grain and glow. No text, letters or numbers anywhere. Keep generous empty pockets for later labels; no boxes, no brown.

Typography stage: "Connect", "Transfer", "Wait", "Sleep" in Figma Inter Bold.

## mobility-observation-path.png

Create a rich editorial illustration for a B2B telecom brand. **Fill the frame
with a warm colour field** — a soft gradient built from Spenza orange
`#C63600`, `#FF4500`, `#FF6F39`, `#FF8E64` — never a plain white
background. Add a fine grain or paper texture across the whole image and a
soft radial glow behind the focal subject so it sits forward. Shapes are
confident and slightly hand-drawn rather than mechanically precise, with
layered depth: a shadow side, a lit side, and a subtle highlight. Icons and
objects are simple and bold, in off-white `#FAFAFA` and ink `#282829`, with
blue-grey `#7B8898` for secondary detail. Generous negative space around a
single clear focal point. No stock-photo people, no clip-art, no flat
outlined rectangles, no drop-shadowed bevels, no 3D renders. 1600×900 pixels,
16:9. Any text is large, minimal and correctly spelled.

A proposed mobility field test in context. A small connected delivery vehicle moves along a curved road between two cellular masts with overlapping translucent coverage arcs. Above the road a separate application cloud receives dotted message marks, with one open gap in the dotted path to signal that delivery must be verified. Two magnifying glasses focus on different evidence: one at a radio mast, one over a message at the cloud. No success checkmarks, no performance claims, no routes on a real map. Bold off-white vehicle and cloud, cool slate masts, warm four-stop orange environment, paper grain and short shadow sides. Leave top y=40..130 and bottom y=780..900 clear for separate typography. No text, letters, numerals, brown terrain or boxed labels.

Typography stage: "Radio record", "Application record" in Figma Inter Bold.
