# Spenza — WordPress → Astro migration

A faithful Astro rebuild of the Spenza WordPress site (`preprod.spenza.com`).

The site is mirrored rather than reinterpreted: every page ships the exact
Elementor/Astra markup and stylesheet set the WordPress page served, so the UI
and animations match. WordPress' jQuery runtime is replaced by a small vanilla
shim, and a thin polish layer smooths the motion.

## Commands

| Command | Action |
| :------ | :----- |
| `npm run dev` | Dev server on <http://localhost:4321> |
| `npm run build` | Static build to `./dist/` |
| `npm run preview` | Preview the build |
| `npm run wp:audit` | Verify every asset each mirrored page references resolves |
| `npm run wp:audit-links` | Verify every internal link resolves |
| `npm run wp:optimize-css` | Rebuild the purged CSS bundles (see below) |

## Architecture

```
src/
  layouts/WpLayout.astro     Mirrored pages: WP stylesheets + chrome + shim
  layouts/BaseLayout.astro   Hand-built pages (the /preview rebuild)
  partials/<slug>.html       Extracted Elementor page content
  partials/chrome/           Header + footer templates (shared by all pages)
  partials/archives/         Category / author / date archives
  partials/posts/            Mirrored Elementor single-post bodies, one per post
  data/wp-styles.json        Per-page stylesheet list, in WordPress' order
  data/wp-inline-styles.json Per-page bundle of extracted inline <style> CSS
  data/wp-scripts.json       Per-page custom site scripts + JSON-LD
  data/wp-pages.json         Titles, meta descriptions, body classes, routes
  data/wp-posts.json         Post slug → route, category and data key
  data/wp-redirects.json     WordPress' 301s, old URL → current URL
  data/blog-categories.ts    Category name → URL slug map
public/
  wp-assets/                 Mirrored Elementor/Astra CSS, fonts, icons, WebP
  wp-content/uploads/        Mirrored media
  scripts/wp-shim.js         Replaces Elementor's jQuery frontend runtime
  scripts/page/<slug>.js     The site's own inline scripts, per page
```

### Why the data files exist

Elementor's cascade is order-dependent, so `wp-styles.json` preserves the exact
stylesheet order each page loaded. Astra's dynamic theme CSS and the site's
`wp-custom-css` only exist as inline `<style>` blocks — including some in the
**body**, which carry real layout rules (the expanding case-study row) — so they
are extracted into `wp-inline-styles.json` bundles. Astra and Elementor also key
styling off body classes, which is why those are mirrored verbatim.

### CSS is bundled and purged, not served as WordPress served it

WordPress linked ~61 stylesheets per page, averaging **2.6 MB of
render-blocking CSS** — the three flaticon icon sets alone are 607 KB on every
page, for a site that uses a few dozen icons.

`wp:optimize-css` concatenates each page's stylesheets *in WordPress' order*,
purges what the page's markup and scripts cannot reference, and minifies. Output
is two tiers, because the inputs behave differently:

- **shared** — the linked stylesheet list. Only 43 distinct lists exist across
  487 pages (238 posts share one), so these become 43 files that stay cached as
  a reader moves between pages.
- **per-page** — the page's inline `<style>` bundle (Astra's dynamic CSS plus the
  Elementor per-post block, ~146 KB raw). This is what makes every page unique.

The cascade survives the split because `WpLayout` already emitted the inline
block last, so shared-then-page reproduces the original order exactly.

Purging is driven by the mirrored markup, the body classes, `wp-shim.js` and each
page's own script bundle, plus a safelist of classes only ever added at runtime.
**Re-run it after anything that changes markup or CSS** — bundles are content-
addressed, so a stale one is silently wrong. Pages the optimiser skips or fails
on keep their original stylesheet list rather than shipping partial CSS.

### URL structure

Blog posts live at their WordPress URLs, `/<category>/<slug>/` (e.g.
`/telecom/what-is-mvno/`), generated by `src/pages/[category]/[slug].astro`.
`/blog/<slug>/` is kept as a redirect, matching WordPress' 301.

### Posts are mirrored, not rebuilt

A post is not an Astra article — it renders through an Elementor `single-post`
template that supplies the breadcrumb, hero, table of contents, author box,
share buttons and the related-posts grid. Rebuilding a post from its markdown
body drops all of that, so `partials/posts/<slug>.html` holds the mirrored
Elementor block and `[category]/[slug].astro` only wraps it in `WpLayout`.

The post list comes from the WordPress REST API rather than the local markdown,
so the mirror cannot silently fall behind what WordPress publishes. The markdown
in `src/content/blog/` is no longer what the site renders.

### Redirects

Post slugs and page paths have been renamed over the years, and the blog content
still links to the old URLs. `wp:redirects` crawls the mirror's own links, finds
the ones it cannot serve, and records what WordPress does with them — recording
a redirect only when its target is a page this site actually serves.

## Re-syncing from WordPress

The pipeline is reproducible. It reads `.wp-cache/cookies.txt` (a logged-in
WordPress session — `.wp-cache/` is gitignored and must never be committed).

```sh
npm run wp:resync        # assets → content → inline CSS → WebP → scripts → pages
npm run wp:posts         # every blog post and case study
npm run wp:archives      # category / author / date archives
npm run wp:webp          # WebP Express variants for whatever the above added
npm run wp:redirects     # WordPress' 301s (needs a preview server, see below)
```

Order matters. `wp:posts` must run before `wp:archives` (which reads the post
partials to discover the author archives) and before `wp:webp` (posts reference
thousands of WebP Express variants that live outside `wp-content/uploads`, so
the media pass alone does not fetch them — skipping this leaves every
`<picture>` in the blog showing a broken image).

`wp:redirects` crawls the built site, so build and serve it first:

```sh
npm run build && npx astro preview --port 4322 &
npm run wp:redirects && npm run build
```

Individual steps are also available: `wp:assets`, `wp:content`, `wp:inline-css`,
`wp:webp`, `wp:scripts`, `wp:pages`.

After any resync, run both audits — they are the regression net:

```sh
npm run build && npm run wp:audit && npm run wp:audit-links
```

## Known gaps

- **Forms don't submit.** Gravity Forms markup is preserved but its runtime is
  not; submissions need a backend or a form service.
- **No analytics or chat.** Tracking tags (GA, LinkedIn, Apollo, Vector) and the
  chat widget were deliberately not carried over.
- **`proxima-nova` is never served.** Headings request it, but WordPress loads it
  from a Google Fonts URL that 403s — it only renders where the font is installed
  locally. Manrope carries the fallback.
- **Content still points at preprod.** Some blog posts link to
  `api.preprod.spenza.com`, `telematics.preprod.spenza.com` and
  `@preprod.spenza.com` addresses.
- **Anchor links are dead.** `#products`, `#solutions`, `#case-studies` have no
  matching element — true on the live WordPress site as well.
